server:
  port: 8889

spring:
  profiles:
    active: git

  cloud:
    config:
      server:
        git:
          uri: https://github.com/LaMinhTam/config-social-media-repo.git
          clone-on-start: true